const state = {
      // 收藏列表
      collect:[]
}

const mutations = {
      setCollect(state){
            return state.collect;
      }
}

const getters = {
      
}

// 异步操作
const actions = {
     
}

export default {
      namespaced: true,
      state,
      actions,
      mutations,
      getters
}